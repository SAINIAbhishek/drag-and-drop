import { DraggingHandleDirective } from './dragging-handle.directive';

describe('DraggingHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggingHandleDirective();
    expect(directive).toBeTruthy();
  });
});
